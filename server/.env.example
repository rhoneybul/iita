# Anthropic — used by /parse-week to slot free-text/voice into the grid.
ANTHROPIC_API_KEY=

# Supabase (service role) — only if you wire up cross-device sync.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Server config
PORT=4000
